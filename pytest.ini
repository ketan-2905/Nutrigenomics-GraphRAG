[pytest]
testpaths = tests
pythonpath = .
addopts = -ra -q
markers =
    unit: unit tests that do not need Docker services
    integration: tests that need Neo4j/Redis/API services
    slow: slower tests
